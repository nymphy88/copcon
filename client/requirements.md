## Packages
framer-motion | Smooth layout transitions and entering animations
react-hook-form | Form state management for agent config and inputs
zod | Schema validation
@hookform/resolvers | Zod resolver for react-hook-form
clsx | Conditional class names
tailwind-merge | Merging tailwind classes intelligently
lucide-react | Icons
date-fns | Date formatting for logs and timestamps

## Notes
API endpoints match the provided schema (conversations, agents, messages, turns).
Auto mode requires frontend state to loop calls to `api.turns.run`.
Dark mode is the default aesthetic.
